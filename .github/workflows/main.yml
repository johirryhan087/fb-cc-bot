# This GitHub Actions workflow is intentionally disabled.
# Reason: Deployment and running are handled exclusively via Railway.
# Do not modify this file to run jobs.

name: Nayan-Bot

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  no_run:
    if: false  # Hard-disabled: No job will ever run
    runs-on: ubuntu-latest

    steps:
      - name: Disabled Job
        run: echo "This workflow is disabled. Deployment is managed on Railway only."
